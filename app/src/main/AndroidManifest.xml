<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.adityaprakash.shopmaking">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".Main2Activity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">

        </activity>
        <activity android:name=".DayWise.CustomerAllDetails" />
        <activity android:name=".DayWise.DateActivity" />
        <activity android:name=".DayWise.DayWiseCustomers" />
        <activity android:name=".invoiceGenerator.CartActivity" />
        <activity android:name=".invoice.CartItemsActivity" />
        <activity android:name=".CustomerActivity" />
        <activity android:name=".BarCodeSearchActivity" />
        <activity android:name=".SearchLocation" />
        <activity android:name=".SearchTypeActivity" />
        <activity android:name=".SearchDetailsActivity"
            android:parentActivityName=".SearchActivity" />
        <activity android:name=".SearchActivity" />
        <activity android:name=".ItemsActivity" />
        <activity android:name=".SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>
        <activity android:name=".SignUpActivity" />
        <activity android:name=".BarcodeScannerActivity" />
        <activity android:name=".LoginActivity" />
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar" />

        <meta-data
            android:name="com.google.firebase.ml.vision.DEPENDENCIES"
            android:value="barcode" />
    </application>
    <uses-permission android:name="android.permission.RECEIVE_SMS" />

    <uses-permission android:name="android.permission.SEND_SMS" />

</manifest>